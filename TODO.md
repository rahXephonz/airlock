# TODO

Deadline **3 Sept 2026, 1:00pm PDT**, then everything freezes until winners are
announced (~23 Sept). Nothing may be touched after submission — not the repo,
not the Devpost entry, not the live sites.

Status as of 28 Aug: the thesis is proven end to end. Cross-origin federation
across four deployed origins works in Brave 151, the policy engine blocks the
exfiltration with a provenance-based reason, and the same page degrades honestly
in ChatGPT's in-app browser. What remains is one policy gap, one mandatory
robustness case, and packaging.

---

## P0 — cannot submit without these

- [ ] **1. Provenance-labelled consent path**
      AGENT.md §1 says Airlock "refuses to escalate without explicit,
      provenance-labelled user consent". Blocks are currently terminal, with no
      escalation at all. A blocked call must be overridable **only from the
      console UI, by a person, with the taint chain shown in Airlock's own
      words** — never by the agent asking for it. This is the sharpest contrast
      with the ChatGPT transcript, where the user was asked to approve
      exfiltration in the seller's own sales copy.

- [ ] **2. Graceful degradation when a partner is unreachable**
      AGENT.md §4 calls this mandatory and it has never been tested. If `vault`
      is down when a judge opens the page, the console must show it as
      unavailable — not hang, not blank, not throw. Test by pointing a frame at
      a dead origin.

- [ ] **3. README with an architecture diagram**
      Four origins and the trust boundary. A judge reading the repo alone has to
      understand the federation without watching anything. Must also carry the
      novelty answer and the silent-failure findings.

- [ ] **4. LICENSE file** detectable in the GitHub About section.

- [ ] **5. Public GitHub repo** — push, confirm the About section shows the
      licence.

- [ ] **6. Testing instructions** — which browser, which flag, what to expect in
      each. Brave 151 works without a flag; Chrome needs
      `chrome://flags/#enable-webmcp-testing`; ChatGPT's browser degrades.

- [ ] **7. Devpost description** — the novelty objection answered in paragraph
      one: why a per-call confirmation generated by the model, covering one
      origin, with no policy surviving the call, is not the same thing.

- [ ] **8. Video, under 3 minutes** (recorded by Ky) — audio, no copyrighted
      music, one continuous narrative rather than a feature tour. Arc per
      AGENT.md §6: task needs three origins, console discovers them, shows trust
      classification, bazaar attempts injection, policy blocks and shows why,
      user confirms the legitimate action, audit log replays it.

- [ ] **9. Tag the submitted commit**, then freeze.

## P1 — strengthens the submission

- [ ] **10. Verify `toolchange` across origins** — does locking the vault remove
      the tool from the console without a reload? Currently unknown; the
      "Re-run discovery" button exists as the fallback either way.

- [ ] **11. Replay from the audit log**
      The video arc ends with "audit log replays it" and the feature does not
      exist. The ledger already stores everything needed. Worth building: replay
      proves decisions are kept as *data* rather than prose, which is exactly the
      contrast with a confirmation dialog that scrolls away.

- [ ] **12. Freeze-proofing** — confirm nothing sleeps, expires, or rate-limits
      across three weeks on the free tier. All four sites are on one Netlify
      account, one billing state, no external API dependency.

## Done

- [x] Spike closed — both browsers measured, findings recorded before any
      product code
- [x] Four origins deployed on one Netlify account
- [x] Trust model, resolver interface, taint tracking, policy engine — 8 tests
- [x] Three fixtures: state-driven registration, declarative form, signal
      passthrough, structured errors, one deliberately dishonest tool
- [x] Console: discovery, mediated proxies, consent queue, ledger, simulated
      fallback
- [x] Mediation proven structural — the agent sees only `airlock_*` proxies
- [x] Attack blocked end to end in ChatGPT's in-app browser, after full
      user consent
- [x] Attack blocked end to end across four real origins in Brave
- [x] Four silent-failure defects found and documented in `docs/FINDINGS.md`
- [x] Netlify badge disabled on all four sites
