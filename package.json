{
  "name": "airlock",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:console": "pnpm --filter @airlock/console dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r exec tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
