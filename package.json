{
  "name": "airlock",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:console": "pnpm --filter @airlock/console dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r exec tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^6.4.3",
    "vitest": "^3.2.7"
  }
}
